/**
 * The single seed identity shared by bun tests (`tests/testHelper.ts`) and the
 * test-mode boot seed (`testMode/seed.ts`) — one identity, one password
 * policy, no second seeding regime.
 */
export const TEST_PASSWORD = "TestPassword123!";
export const TEST_ADMIN_EMAIL = "admin@shade-test.com";
export const TEST_USER_EMAIL = "user@shade-test.com";

export const TEST_CHANNEL_NAME = "test-harness-channel";
export const TEST_GROUP_NAME = "test-harness-group";
export const TEST_GROUP_EXTERNAL_ID = "test-harness-group";
